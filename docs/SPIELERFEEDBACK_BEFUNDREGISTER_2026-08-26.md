# Spielerfeedback — Befundregister 2026-08-26

**Auslöser:** Adrian meldet 6 Live-Befunde direkt aus der Spieler-Perspektive (Kampagnen-Buch +
Spieltisch), mit dem ausdrücklichen Vorrang „live Spieler-Feedback geht vor geplanter Arbeit
[MASTERPLAN_1_100_2026-08-22.md]". Hausregel gilt (Adrian, 2026-08-25, [[feedback-no-finding-is-low]]):
**jeder Befund einzeln registriert, Severity schließt nichts, jeder Befund endet in Fix, Robot-Netz
oder dokumentierter Entscheidung — „unverifiziert" ist keine zulässige Endstation.**

Alle 7 Befunde unten wurden gegen den echten HEAD-Code verifiziert (nicht gegen Doku-Behauptungen).
5 davon waren tatsächlich noch offen und sind jetzt gefixt + getestet; einer war schon vor Adrians
Meldung bekannt und bewusst zurückgestellt (F1); einer kam erst beim Fixen selbst zum Vorschein (F7).

## Register

| # | Befund | Severity | Beleg | Ende |
|---|---|---|---|---|
| F1 | „Hub öffnen"/„Vorbereitung" auf jeder Kampagnenkarte navigiert hart in die alte, vor-Redesign „Klassik"-Seite (dunkelblaues Inline-CSS, eigene Nav) statt im Buch-UI zu bleiben — **bereits gestern in [DESKTOP_AUDIT_BROWSER_JOURNEY_2026-08-25.md] gefunden (D02/D03/D09/D10) und in [FIX_RESEARCH_DESKTOP_AUDIT_2026-08-25.md] §3 als C1 bewusst zurückgestellt** — Adrian ist der erste echte Nutzer, der es getroffen hat | **high** | `book-scene.js` `buildIntentHref()`/`buildCampaignLedger()`, `campaigns.html` `bootstrapCampaignsScene()` (klassischer Fallback bei `?campaign_id=`) | **Fix** (interim-Umfang von C1): Hub rendert jetzt in-book (`renderCampaignHubDetail`), Klassik-Markup als `<template>` inertisiert (löst D09 nebenbei); Playwright-Szenario `campaign_hub_click_flow` neu, 0 Funde. Voller C1-Umbau (alle Klassik-Reste entfernen) bleibt offen wie gestern schon gescoped |
| F2 | Keine Möglichkeit, eine Kampagne oder einen Charakter bei Erstellung als öffentlich/privat zu markieren — Basis für künftige Spielersuche, aber „privat" muss auch wirklich privat sein | medium | `Campaign`/`Character`-Modelle hatten kein Sichtbarkeitsfeld; `GET /api/campaigns` gab vorher jede nicht gelöschte Kampagne an jeden eingeloggten Nutzer zurück (unabhängig von diesem Ticket — ein bestehendes Leck) | **Fix**: neue Spalte `is_discoverable` (bewusst NICHT `is_public` — kollidiert mit `Campaign.is_public()`, einer unabhängigen „nicht gelöscht"-Methode), Default `false`, Checkbox in beiden Erstellungsformularen, `list_campaigns` filtert jetzt wirklich (fremde private Kampagnen verschwinden aus der Browse-Liste). Migration `migrations/migration_m68_campaign_character_discoverability.sql` — **muss noch von Hand gegen Staging/Prod laufen** (`psql "$DATABASE_URL" -f migrations/migration_m68_campaign_character_discoverability.sql`), `AUTO_CREATE_SCHEMA` legt keine Spalten auf bestehenden Tabellen an |
| F3 | Eigene Würfelwürfe am Tisch (nicht Beyond20) erscheinen live nicht im Chat — nur im Würfel-Widget, Chat holt es erst nach Reload aus der DB nach | medium | `play-ui.js` `_handleDiceBroadcast` rief `_appendChatMessage` nie auf, anders als das Beyond20-Gegenstück `_handleExternalRoll`; Server-Persistenz war bereits korrekt | **Fix**: fehlender `_appendChatMessage`-Aufruf ergänzt, Robot-Flow-Assertion auf `#chatLog` ergänzt (Muster vom Beyond20-Flow übernommen) |
| F4 | (a) Spieler kann kein Bild auf das eigene Token setzen — Upload-Endpunkt verlangt DM/Co-DM-Rechte, obwohl der Socket-Patch-Pfad Besitzer schon korrekt erlaubt. (b) Kein Auswahl-/Kontrollmenü — Token-Auswahl setzt nur einen CSS-Ring, öffnet aber nie das Steuerungs-Panel | **high** | `assets.py` `upload_asset` (`can_edit_campaign`-Gate), `play-ui.js` `_selectToken()` öffnet `#tokenWidget` nie | **Fix**: Upload für `asset_type=token` jetzt für aktive Mitglieder offen (alle anderen Asset-Typen bleiben DM/Co-DM-only); `_selectToken()` öffnet jetzt automatisch das Token-Panel (dieselbe Logik wie der bestehende `_openTokenMenu()`); Umbenennen-Feld ergänzt (fehlte komplett). Offene Frage unten (Moderation) |
| F5 | Schwebende Fenster am Spieltisch lassen sich nicht per Drag verschieben | medium | 4 Widgets (`#layersWidget`, `#turnOrderWidget`, `#tokenWidget`, `#tokenCreatePanel`) waren CSS-fixiert, keine gemeinsame Drag-Komponente vorhanden | **Fix (Teilumfang)**: gemeinsames Drag-Modul, alle 4 bestehenden Panels per Kopfzeile verschiebbar, Position pro Session in `sessionStorage`, inert unter 1040px. **Nicht enthalten** (Umfangsgrenze, siehe Entscheidung unten): Chat/Journal/Würfel-Log sind heute Tabs in EINER Sidebar, kein eigenes Fenster; Charakterbogen-Drawer ebenso kein Floating-Fenster |
| F6 | Kein nativer Weg, den eigenen D&D-Beyond-Charakter als JSON zu importieren | — | Recherche: Foundrys „DDB Importer" nutzt Cookie-Scraping über einen Proxy (inoffiziell, ToS-grau); Roll20 nutzt ebenfalls nur die Beyond20-Extension, kein offizieller WotC-Sync; Beyond20 selbst exportiert heute keine volle Charakterdatei. Roll Drauf hat bereits eine funktionierende Import-Pipeline (`POST /api/characters/import`), aber nur fürs eigene Homebrew-System (`roll_drauf_light`) — Spezies/Klasse/Hintergrund/Fraktion sind eigene Kataloge, „Fraktion" hat in D&D 5e keine Entsprechung | **Entscheidung nötig** (siehe unten) — nichts implementiert |
| F7 | *(beim Fixen von F3 entdeckt, nicht von Adrian gemeldet)* Der komplette `dice_roll_realtime`-Playwright-Robot war seit dem letzten Commit vor diesem Batch (`e834659`, Werkzeugleiste in die Sidebar-Schublade verschoben) stumm kaputt — `#diceInput`/`#btnRoll` liegen in der standardmäßig geschlossenen rechten Sidebar, der Flow wurde nie auf „erst öffnen" angepasst (im Gegensatz zu `_map_token_table_flow`, der das schon konnte) | medium | `tools/robots/flows.py::_dice_roll_flow` — Timeout auf `#diceInput`, per `git stash`-Bisect als vor-F3 bestehend bestätigt | **Fix**: gleiche zwei Zeilen wie `_map_token_table_flow` (`#btnSidebarToggle` klicken, `.right-sidebar.is-open` abwarten) ergänzt; alle 4 registrierten Flows (`dice_roll_realtime`, `map_token_table`, `campaign_hub_click`, `beyond20_bridge`) laufen jetzt wieder durch, 0 Funde |

## Verifikation

- Vollständige Testsuite: **492 passed, 0 failed** (`pytest tests/`, zweimal gegenlaufen).
- Browser-Robot-Flows (`python -m tools.robots.flows`): **4/4 grün, 0 Funde** (inkl. des neuen F1-Szenarios und des reparierten F7-Flows).
- Nichts committet, nichts deployed — Migration nur gegen lokales Dev-SQLite angewendet, Staging/Prod bewusst nicht angefasst (etablierte Praxis dieses Repos: manuelle DB-Migration, kein automatischer Deploy).
- Ein unverwandtes, unstaged Dokument liegt im Baum: `docs/MERCH_TSHIRT_RESEARCH_2026-08-26.md` — nicht Teil dieses Batches, nicht angefasst (vermutlich eine parallele Session/Codex-Arbeit).

## Offene Entscheidungen für Adrian

1. **F1 — vollständiger C1-Umbau jetzt einplanen?** Der Interim-Fix macht „Hub öffnen" wieder Buch-UI-konsistent, aber die Klassik-Seite existiert (inertisiert) weiterhin, und zwei weitere Stellen navigieren noch hart dorthin (Join-Erfolg-Handler, Nicht-DM-„Kampagne öffnen"-Knopf in `renderPlayLaunch`) — gleiche Fehlerklasse, nicht in diesem Batch angefasst. Soll C1 (eine Render-Engine, Klassik-Reste ganz raus) jetzt terminiert werden?
2. **F2 — Migration ausführen.** `psql "$DATABASE_URL" -f migrations/migration_m68_campaign_character_discoverability.sql` muss von Hand gegen Staging und Prod laufen, bevor die Checkbox dort etwas bewirkt (sonst 500 beim ersten Zugriff auf `is_discoverable`).
3. **F4(a) — Moderation für Spieler-Uploads?** Der Fix ist bewusst permissiv (jedes aktive Mitglied kann Token-Bilder hochladen, ungeprüft, wie es die RBAC-Doku ohnehin verspricht) — reicht das, oder braucht es eine DM-Freigabe/Vorauswahl, weil beliebige Bilddateien in die Asset-Bibliothek eines geteilten Tisches wandern?
4. **F5 — Umfang B?** Chat/Journal/Würfel-Log als eigene, frei bewegliche Fenster aus der Sidebar herauslösen (plus Charakterbogen) ist ein spürbar größerer Layout-Umbau als das, was jetzt gefixt ist — eigene Runde, oder jetzt mit einplanen?
5. **F6 — Grundsatzfrage.** Ist Cookie-basiertes Scraping von D&D Beyond (à la ddb-importer/ddb-proxy) überhaupt akzeptabel, oder ist manueller JSON-Einfügen die harte Obergrenze? Und: soll v1 überhaupt D&D-Beyond-spezifisch sein, oder reicht „beliebiges Charakter-JSON einfügen" mit einer D&D-Beyond-Zuordnung als einem von mehreren Adaptern? „Fraktion" hat keine 5e-Entsprechung — freies Feld, Pflichtfeld mit Default, oder Rückfrage an den Spieler beim Import?
